apiVersion: apps/v1
kind: Deployment
metadata:
  name: backend-deployment
spec:
  replicas: 1
  selector:
    matchLabels:
      app: backend
  template:
    metadata:
      name: backend-pod
      labels:
        app: backend
    spec:
      containers:
        - name: backend
          image: badhon58/jobportalbackend:v1
          ports:
            - containerPort: 5000
          env:
            - name: PORT
              value: 5000
            - name: DBURL
              value: mongodb+srv://biswasbadhon896_db_user:gRbnlE3pi1tOUC27@jobportal.rfs6gno.mongodb.net/?appName=jobportal
            - name: SECRET_KEY
              value: BadhonBiswas
---
apiVersion: v1
kind: Service
metadata:
  name: backend-service
spec:
  type: NodePort
  selector:
    app: backend
  ports:
    - port: 5000
      targetPort: 5000
      nodePort: 30005
